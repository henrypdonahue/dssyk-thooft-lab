# Exact pins: eigh(D, S) is version-sensitive at the ~12th digit, and the
# reference tables were certified with these versions (Python 3.9).
numpy==2.0.2
scipy==1.13.1

# Arbitrary-precision solver (thooft_highprec.py, generate_reference.py)
mpmath==1.3.0

# Regge-trajectory plot (plot_spectrum.py)
matplotlib==3.9.4

# Test suite (test_thooft.py)
pytest==6.2.5
