# Draft note: exact self-averaging statistics + dictionary corrections

`main.tex` — a ~6-page standalone manuscript of the repo's exact results
(`exact_wick.py` closed forms, triple verification, double-scaling and fixed-p
consequences, and the corrected Mn/CP operator dictionary). Figure:
`self_averaging.png` (copied from `syk-self-averaging/`; regenerate there).

**Status: draft for the author's review — not for distribution.** Two `TODO(author)`
markers (affiliation, acknowledgments) must be resolved before circulating.

No TeX toolchain on this machine: compile on Overleaf (upload both files) or with
`pdflatex main.tex` (twice, for references) anywhere TeX Live is installed.
Every displayed equation is machine-verified in the repo test suites.
